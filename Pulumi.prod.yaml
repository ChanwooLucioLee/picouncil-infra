config:
  aws:region: ap-northeast-2
  picouncil-infra:instanceType: t4g.micro
  # Set these secrets via:
  # pulumi config set --secret database-url "postgres://..."
  # pulumi config set --secret jwt-secret "..."
  # pulumi config set --secret cloudflareTunnelToken "..."  (optional, for custom domain)
